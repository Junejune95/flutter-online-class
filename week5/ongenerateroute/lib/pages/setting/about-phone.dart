import 'package:flutter/material.dart';

class AboutPhonePage extends StatelessWidget {
    static const routeName = "/about-phone-page";
  @override
  Widget build(BuildContext context) {
    return Scaffold( );
  }
}